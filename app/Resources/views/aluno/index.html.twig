{#
    {% extends ':template:template.html.twig' %}

{% block stylesheets %}

    {{ parent() }}

        <link rel="stylesheet" href="{{ asset("bundles/mrssge/css/dataTables.bootstrap.css") }}">

{% endblock %}


    {% block contentheader %}


    <section class="content-header">
        <h1>
            Aluno
            <small>Listar</small>
        </h1>
    </section>


    {% endblock %}


    {% block panelheading %}
    <a href="{{ path('cadastro_aluno_new') }}" class="btn btn-primary btn-sm link-search">
            <span class="glyphicon glyphicon-plus"></span>
            Novo
        </a>    {% endblock %}

    {% block panelbody %}
#}
<div class="table-responsive">
    <table class="table table-bordered table-condensed table-hover table-striped table-datatable">
        <thead>
        <tr>
            <th>Ações</th>
            <th>Nome</th>
            <th>Datanascimento</th>
            <th>Valormensalidade</th>
            <th>Vencimento</th>
            <th>Serie</th>
            <th>Sala</th>
            <th>Status</th>
        </tr>
        </thead>
        <tbody>
        {% for aluno in alunos %}
            <tr>
                <td>
                    <a href="{{ path('cadastro_aluno_show', { 'id': aluno.id }) }}" class="btn btn-primary btn-sm link-search">
                        <span class="glyphicon glyphicon-eye-open"></span>
                        Mostrar
                    </a>
                    <a href="{{ path('cadastro_aluno_edit', { 'id': aluno.id }) }}" class="btn btn-primary btn-sm link-search">
                        <span class="glyphicon glyphicon-edit"></span>
                        Editar
                    </a>
                </td>
                <td><a href="{{ path('cadastro_aluno_show', { 'id': aluno.id }) }}">{{ aluno.nome }}</a></td>
                <td>{% if aluno.dataNascimento %}{{ aluno.dataNascimento|date('Y-m-d') }}{% endif %}</td>
                <td>{{ aluno.valorMensalidade }}</td>
                <td>{% if aluno.vencimento %}{{ aluno.vencimento|date('Y-m-d') }}{% endif %}</td>
                <td>{{ aluno.serie }}</td>
                <td>{{ aluno.sala }}</td>
                <td>{{ aluno.status }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>


{#
{% endblock %}


{% block javascripts %}
{{ parent() }}

    <script src="{{ asset("bundles/mrssge/js/jquery.dataTables.js") }}"></script>
    <script src="{{ asset("bundles/mrssge/js/dataTables.bootstrap.js") }}"></script>
    <script src="{{ asset("bundles/mrssge/js/mrs-dataTable.js") }}"></script>


{% endblock %}

#}